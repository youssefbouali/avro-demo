version: '3.8'

services:
  avro-api:
    build:
      context: .
      dockerfile: Dockerfile.avro
    ports:
      - "5000:5000"
    environment:
      FLASK_ENV: development
      FLASK_APP: avro_rest_api.py
    volumes:
      - .:/app
    restart: unless-stopped
